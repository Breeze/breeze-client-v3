import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.DYAmjhAm.js";const c=JSON.parse('{"title":"Navigation properties","description":"","frontmatter":{},"headers":[],"relativePath":"guide/navigation-properties.md","filePath":"guide/navigation-properties.md","lastUpdated":1789754513000}'),n={name:"guide/navigation-properties.md"};function r(o,e,h,d,l,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="navigation-properties" tabindex="-1">Navigation properties <a class="header-anchor" href="#navigation-properties" aria-label="Permalink to &quot;Navigation properties&quot;">​</a></h1><p>Entities are connected by associations. In Northwind, an <code>Order</code> has a parent <code>Customer</code> and many child <code>OrderDetail</code>s. An <code>OrderDetail</code> has a parent <code>Order</code> and a parent <code>Product</code>. You move around this graph by following <strong>navigation properties</strong>: <code>customer</code> → <code>orders</code> → <code>orderDetails</code> → <code>product</code>.</p><p>There are two kinds:</p><table tabindex="0"><thead><tr><th>Kind</th><th>Example</th><th>Returns</th></tr></thead><tbody><tr><td>scalar (reference)</td><td><code>order.customer</code></td><td>one entity, or <code>null</code></td></tr><tr><td>collection</td><td><code>customer.orders</code></td><td>an array of zero or more entities</td></tr></tbody></table><h2 id="reading" tabindex="-1">Reading <a class="header-anchor" href="#reading" aria-label="Permalink to &quot;Reading&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> customer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.customer;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Customer or null</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orders</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> customer.orders;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// RelationArray&lt;Order&gt;</span></span></code></pre></div><p>With the backing-store model library, which is the only one in Breeze 3, navigation properties are plain properties. The examples on this page assume you have registered entity classes, so they are type-checked — see <a href="/breeze-client-v3/guide/typed-entities">Typed entities</a>. Without classes the same reads are <code>order.getProperty(&#39;customer&#39;)</code> and <code>customer.getProperty(&#39;orders&#39;)</code>, which work identically and return <code>any</code>.</p><p>A navigation property returns only entities that are <strong>in the cache</strong>. If <code>order.customer</code> is <code>null</code>, the customer isn&#39;t cached. It may still exist on the server. An empty <code>orders</code> array means no orders for that customer are cached right now. A collection also leaves out related entities that have been deleted. To collect an entity with everything it reaches, deleted ones included, use <code>getEntityGraph</code> from the <a href="/breeze-client-v3/guide/extensions#entity-graphs">entity graphs</a> extension.</p><p>A collection navigation property returns a Breeze relation array. That is a real array, with an <code>arrayChanged</code> event and a <code>load()</code> method (see <a href="/breeze-client-v3/api/interfaces/RelationArray"><code>RelationArray</code></a>).</p><h2 id="setting" tabindex="-1">Setting <a class="header-anchor" href="#setting" aria-label="Permalink to &quot;Setting&quot;">​</a></h2><p>Set a scalar navigation property as you would any other property:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.customer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> anotherCustomer;</span></span></code></pre></div><p>Breeze keeps both ends of the association in step. After that line, <code>order</code> has left the old customer&#39;s <code>orders</code> array, and joined <code>anotherCustomer.orders</code>. <code>order.customerID</code> now holds the new customer&#39;s key.</p><p>You can make the same change from the other end, by pushing to the collection:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">anotherCustomer.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(order);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// order.customer === anotherCustomer</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// order.customerID === anotherCustomer&#39;s key</span></span></code></pre></div><p>Remove an entity from a collection with <code>splice</code>, <code>pop</code> or <code>shift</code>. That sets the child&#39;s navigation property to <code>null</code>, and clears its foreign key.</p><p>You can&#39;t replace the collection itself:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [];</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Error: Nonscalar navigation properties are readonly - entities can be added or</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// removed but the collection may not be changed.</span></span></code></pre></div><p>With a registered class the compiler stops this first: a generated collection navigation is declared <code>RelationArray&lt;Order&gt;</code>, which a plain array is not. Without classes it throws at run time, as above.</p><h3 id="attaching-as-a-side-effect" tabindex="-1">Attaching as a side effect <a class="header-anchor" href="#attaching-as-a-side-effect" aria-label="Permalink to &quot;Attaching as a side effect&quot;">​</a></h3><p>If you connect a cached entity to a detached one, Breeze adds the detached entity to the cache as <code>Added</code>, whichever side you set. For example, setting a detached order&#39;s <code>customer</code> to a cached customer brings the order into that customer&#39;s manager. An entity can&#39;t be connected to an entity in a different <code>EntityManager</code>; if you try, Breeze throws.</p><h2 id="foreign-keys" tabindex="-1">Foreign keys <a class="header-anchor" href="#foreign-keys" aria-label="Permalink to &quot;Foreign keys&quot;">​</a></h2><p>Breeze associations depend on <strong>foreign keys</strong>. Metadata tells Breeze which data property holds the foreign key for each navigation property, and Breeze uses that to connect entities in the cache.</p><p>In every association, one side is the <strong>principal</strong>, and the other is the <strong>dependent</strong>. The dependent holds the foreign key: a property whose value is the principal&#39;s key.</p><ul><li>In a one-to-many association, the &quot;many&quot; side is the dependent. <code>Order</code> has a <code>customerID</code> foreign key pointing to its <code>Customer</code>. A customer has no foreign keys for its orders.</li><li>An entity can be dependent in one association and principal in another. <code>Order</code> is dependent on <code>Customer</code>, and principal to <code>OrderDetail</code>, whose <code>orderID</code> points back to it.</li><li>In a one-to-one association, either side can be the dependent. The domain decides.</li></ul><p>If your model has no foreign key property, Breeze can&#39;t connect the entities. Navigation properties then stay empty, even when both ends are in the cache.</p><p>A many-to-many association needs a join entity. Model it as two one-to-many associations, the way Northwind does with <code>Employee</code> → <code>employeeTerritories</code> → <code>territory</code>.</p><h3 id="setting-the-foreign-key-directly" tabindex="-1">Setting the foreign key directly <a class="header-anchor" href="#setting-the-foreign-key-directly" aria-label="Permalink to &quot;Setting the foreign key directly&quot;">​</a></h3><p>Setting the foreign key has the same effect as setting the navigation property:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.customerID </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> someCustomerID;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> customer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.customer;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the cached Customer with that key, or null if it isn&#39;t in the cache</span></span></code></pre></div><p>If the customer isn&#39;t cached yet, Breeze remembers the link. When a customer with that key arrives later, through a query or an import, <code>order.customer</code> starts returning it.</p><h3 id="one-sided-associations" tabindex="-1">One-sided associations <a class="header-anchor" href="#one-sided-associations" aria-label="Permalink to &quot;One-sided associations&quot;">​</a></h3><p>You don&#39;t need a navigation property at both ends. For example, you might have <code>Person.gender</code> but no <code>Gender.persons</code> collection. No one needs every <code>Person</code> of a gender, and keeping that collection up to date has a cost. Leave the navigation property out of the principal&#39;s metadata. The foreign key on the dependent is still required.</p><h2 id="no-lazy-loading" tabindex="-1">No lazy loading <a class="header-anchor" href="#no-lazy-loading" aria-label="Permalink to &quot;No lazy loading&quot;">​</a></h2><p>Some data frameworks go to the server the first time you touch a navigation property, and load the related entities automatically. Breeze doesn&#39;t: navigation properties never go to the server. You load related entities explicitly, either with <code>expand</code> on a query, or on demand when you need them.</p><h2 id="eager-loading-with-expand" tabindex="-1">Eager loading with expand <a class="header-anchor" href="#eager-loading-with-expand" aria-label="Permalink to &quot;Eager loading with expand&quot;">​</a></h2><p><code>expand</code> includes related entities in a query&#39;s response:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Order)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;customerID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;==&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, customerID)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDetails&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">results</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">orders</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">executeQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><p>The query still returns orders. Breeze also takes the order details out of the response, merges them into the cache, and connects them to their orders. <code>order.orderDetails</code> is now populated; without the <code>expand</code> it would be empty.</p><p>To expand several paths, separate them with commas, or pass an array. Dots follow a path through several associations:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Order)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;customerID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;==&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, customerID)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;customer, orderDetails.product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><ul><li>Each segment of a path is a navigation property name. Use the client-side spelling. With the default <code>NamingConvention.camelCase</code> that means <code>orderDetails</code>, not <code>OrderDetails</code>.</li><li>Every entity along the path is included. <code>orderDetails.product</code> returns the details <em>and</em> their products.</li></ul><p><code>expand</code> is easy to overuse. Every expansion adds data to the response, and too much can crush the performance of both client and server. Where you can, load related entities on demand instead. If <code>expand</code> returns less than you expect, see <a href="/breeze-client-v3/query/debugging">Debugging queries</a>.</p><h2 id="loading-on-demand" tabindex="-1">Loading on demand <a class="header-anchor" href="#loading-on-demand" aria-label="Permalink to &quot;Loading on demand&quot;">​</a></h2><p>Suppose you list orders without their details. The user picks one, and now you need its details.</p><h3 id="loadnavigationproperty" tabindex="-1">loadNavigationProperty <a class="header-anchor" href="#loadnavigationproperty" aria-label="Permalink to &quot;loadNavigationProperty&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">loadNavigationProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDetails&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // order.orderDetails is now populated</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (err) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // handle the error</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>loadNavigationProperty</code> builds a query for the related entities, runs it, and returns the query&#39;s promise. When the results arrive, they are merged into the cache, and <code>order.orderDetails</code> fills in. A relation array has the same operation, as <code>load()</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.orderDetails.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">load</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>After a successful <code>loadNavigationProperty</code>, <code>entityAspect.isNavigationPropertyLoaded(&#39;orderDetails&#39;)</code> returns <code>true</code>. It also returns <code>true</code> after you call <code>markNavigationPropertyAsLoaded</code>, and for a scalar navigation property whenever its value is not null.</p><h3 id="a-query-of-your-own" tabindex="-1">A query of your own <a class="header-anchor" href="#a-query-of-your-own" aria-label="Permalink to &quot;A query of your own&quot;">​</a></h3><p><code>loadNavigationProperty</code> doesn&#39;t let you add an <code>expand</code>. If you also want each detail&#39;s product, write the query yourself:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(OrderDetail)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;==&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, order.orderID)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">executeQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><h3 id="entityquery-fromentitynavigation" tabindex="-1">EntityQuery.fromEntityNavigation <a class="header-anchor" href="#entityquery-fromentitynavigation" aria-label="Permalink to &quot;EntityQuery.fromEntityNavigation&quot;">​</a></h3><p>That query assumes you know how the association is implemented. <code>fromEntityNavigation</code> builds the same query from metadata instead:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityQuery</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fromEntityNavigation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(order, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDetails&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">executeQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><p>Breeze looks up the <code>orderDetails</code> navigation property, and gets two things from it: the foreign key (<code>orderID</code>) and the resource name (<code>OrderDetails</code>). It then builds a predicate that matches the order&#39;s key.</p><h3 id="related-entities-for-several-parents" tabindex="-1">Related entities for several parents <a class="header-anchor" href="#related-entities-for-several-parents" aria-label="Permalink to &quot;Related entities for several parents&quot;">​</a></h3><p>To load the details of several orders in one request, combine the predicates from their navigation queries:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { EntityQuery, Predicate } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> predicates</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fromEntityNavigation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDetails&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).wherePredicate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(OrderDetail)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Predicate.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">or</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(predicates))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">executeQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><p>Before you run it, consider how much data it will fetch.</p><h2 id="state-and-events-when-you-set-a-navigation-property" tabindex="-1">State and events when you set a navigation property <a class="header-anchor" href="#state-and-events-when-you-set-a-navigation-property" aria-label="Permalink to &quot;State and events when you set a navigation property&quot;">​</a></h2><p>Setting a navigation property on a dependent entity changes two of its properties: the navigation property and its foreign key. The entity raises <code>propertyChanged</code> for each. An <code>Unchanged</code> entity becomes <code>Modified</code>, because the new foreign key has to be saved.</p><p>The principal is <strong>not</strong> changed. When an order joins <code>customer.orders</code>, the customer stays <code>Unchanged</code>, and raises no <code>propertyChanged</code> event. The contents of its collection changed, but none of the customer&#39;s own properties did. The collection raises <code>arrayChanged</code> instead:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.orders.arrayChanged.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // args.array, args.added?, args.removed?</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>From a relational point of view that is right: adding an order doesn&#39;t change any column in the customer&#39;s row. Whether it is right for your domain depends on the domain. Few people think of adding an order as changing the customer, but many think of adding a line item as changing the order. Breeze can&#39;t tell the two cases apart. If your domain treats the parent as changed, call <code>parent.entityAspect.setModified()</code> yourself.</p><p>Deleting an entity removes it from the collections of its related entities, and sets its scalar navigation properties to <code>null</code>. Its foreign key values are kept, so that they can be sent to the server.</p>`,67)])])}const g=s(n,[["render",r]]);export{c as __pageData,g as default};
