import{_ as t,o as s,c as i,a2 as a}from"./chunks/framework.DYAmjhAm.js";const k=JSON.parse('{"title":"Inside the entity","description":"","frontmatter":{},"headers":[],"relativePath":"guide/inside-the-entity.md","filePath":"guide/inside-the-entity.md","lastUpdated":1789853623000}'),n={name:"guide/inside-the-entity.md"};function d(h,e,r,o,l,p){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="inside-the-entity" tabindex="-1">Inside the entity <a class="header-anchor" href="#inside-the-entity" aria-label="Permalink to &quot;Inside the entity&quot;">​</a></h1><p>A domain object such as a <code>Customer</code> has data (<code>companyName</code>), relationships (<code>orders</code>), and perhaps some business logic (<code>isGoldCustomer</code>). Those members are what your application binds to its UI and reasons about.</p><p>A <code>Customer</code> is also an <strong>entity</strong>: a long-lived object with a permanent key. It can be fetched, cached, changed, validated and saved. Questions like &quot;has it changed?&quot;, &quot;what were its values?&quot; and &quot;is it valid?&quot; are about its <em>entity nature</em>. Breeze handles that part, and you reach it through two members on every entity: <code>entityType</code> and <code>entityAspect</code>.</p><h2 id="entitytype" tabindex="-1">entityType <a class="header-anchor" href="#entitytype" aria-label="Permalink to &quot;entityType&quot;">​</a></h2><p><code>entity.entityType</code> returns the entity&#39;s <a href="/breeze-client-v3/api/classes/EntityType"><code>EntityType</code></a>: the metadata that describes its properties, keys and associations.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orderType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.entityType;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderType.shortName;                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;Order&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderType.keyProperties.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> p.name);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [&#39;orderID&#39;]</span></span></code></pre></div><p>See <a href="/breeze-client-v3/metadata/">Metadata</a>.</p><h2 id="entityaspect" tabindex="-1">entityAspect <a class="header-anchor" href="#entityaspect" aria-label="Permalink to &quot;entityAspect&quot;">​</a></h2><p>A Breeze entity is self-tracking. It holds its state, and the means to change that state, in the <a href="/breeze-client-v3/api/classes/EntityAspect"><code>EntityAspect</code></a> returned by <code>entity.entityAspect</code>.</p><p>An object gets its <code>EntityAspect</code>, and keeps it for the rest of its life, when any one of these happens:</p><ul><li>it enters the cache as the result of a query or an import</li><li>it is created by <code>em.createEntity</code> or <code>EntityType.createEntity</code> (see <a href="/breeze-client-v3/guide/creating-entities">Creating entities</a>)</li><li>it is added to, or attached to, an <code>EntityManager</code></li></ul><p>This page covers <code>EntityAspect</code> in four parts: entity state, property change notification, validation, and everything else.</p><h2 id="entitystate" tabindex="-1">EntityState <a class="header-anchor" href="#entitystate" aria-label="Permalink to &quot;EntityState&quot;">​</a></h2><p><code>entityAspect.entityState</code> answers two questions: is the entity in a cache, and if so, has it changed? Its value is one of the <a href="/breeze-client-v3/api/classes/EntityState"><code>EntityState</code></a> values:</p><table tabindex="0"><thead><tr><th>EntityState</th><th>Meaning</th></tr></thead><tbody><tr><td><code>Added</code></td><td>A new entity in the cache that doesn&#39;t exist in the database yet.</td></tr><tr><td><code>Unchanged</code></td><td>An existing entity in the cache, with no changes since it was last queried or saved.</td></tr><tr><td><code>Modified</code></td><td>An existing entity in the cache, with pending changes.</td></tr><tr><td><code>Deleted</code></td><td>An existing entity in the cache that is marked for deletion.</td></tr><tr><td><code>Detached</code></td><td>An entity that isn&#39;t in any cache. Its state in the database is unknown.</td></tr></tbody></table><p>Compare against the enum value, or use the test methods:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { EntityState } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> state</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.entityAspect.entityState;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityState.Modified) { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (state.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isModified</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (state.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isAddedModifiedOrDeleted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;has pending changes&quot;</span></span></code></pre></div><p>The test methods are <code>isAdded</code>, <code>isUnchanged</code>, <code>isModified</code>, <code>isDeleted</code>, <code>isDetached</code>, <code>isAddedOrModified</code>, <code>isUnchangedOrModified</code> and <code>isAddedModifiedOrDeleted</code>. Every enum value also has a <code>name</code> (<code>&#39;Modified&#39;</code>), which is useful for display.</p><h3 id="transitions" tabindex="-1">Transitions <a class="header-anchor" href="#transitions" aria-label="Permalink to &quot;Transitions&quot;">​</a></h3><p>Breeze updates the state as things happen to the entity:</p><table tabindex="0"><thead><tr><th>Before</th><th>Action</th><th>After</th></tr></thead><tbody><tr><td>—</td><td>Materialized in the cache by a query</td><td><code>Unchanged</code></td></tr><tr><td><code>Unchanged</code></td><td>Set one of its properties</td><td><code>Modified</code></td></tr><tr><td><code>Modified</code></td><td>Save it successfully</td><td><code>Unchanged</code></td></tr><tr><td><code>Unchanged</code></td><td><a href="#deleting">Mark it deleted</a></td><td><code>Deleted</code></td></tr><tr><td><code>Deleted</code></td><td>Save it successfully</td><td><code>Detached</code></td></tr><tr><td>—</td><td>Create it with <code>EntityType.createEntity</code></td><td><code>Detached</code></td></tr><tr><td><code>Detached</code></td><td>Add it to a manager</td><td><code>Added</code></td></tr><tr><td><code>Added</code></td><td>Delete it, or <a href="#rejectchanges">reject its changes</a></td><td><code>Detached</code></td></tr></tbody></table><p>Two of these transitions surprise people:</p><ul><li>After you delete an <strong>existing</strong> entity and save, it becomes detached. Breeze can&#39;t make the object disappear, and it may still be on screen. It no longer exists on the server, though, so Breeze removes it from the cache.</li><li>Deleting a <strong>new</strong> entity detaches it immediately. Breeze doesn&#39;t wait for a save, because there is nothing on the server to delete.</li></ul><h3 id="detached-entities" tabindex="-1">Detached entities <a class="header-anchor" href="#detached-entities" aria-label="Permalink to &quot;Detached entities&quot;">​</a></h3><p>A detached entity doesn&#39;t belong to an <code>EntityManager</code>. It is still an entity, just not one in a cache.</p><p>Don&#39;t keep detached entities around. Either attach one to a manager, or drop your references to it so that it can be garbage-collected. A detached entity still has its data values, and you can still set them. But its <a href="/breeze-client-v3/guide/navigation-properties">navigation properties</a> are empty, and you can&#39;t tell by looking at it whether there is a matching record in the database. You also can&#39;t change its state: calling <code>setModified</code>, <code>setDeleted</code> or similar on a detached entity throws.</p><p>New entities often start out detached. Create them, initialize them (if the key isn&#39;t generated, set it, since every entity in a cache needs a unique key), and add them to a manager straight away.</p><p>These actions detach an entity:</p><ul><li><code>em.detachEntity(entity)</code> or <code>entity.entityAspect.setDetached()</code></li><li><code>em.clear()</code></li><li>deleting, or rejecting the changes of, an <code>Added</code> entity</li><li>a successful save of a <code>Deleted</code> entity</li></ul><p>Detaching an entity doesn&#39;t delete it. If it existed in the database, it still does. A detached entity keeps its foreign key values, but its navigation properties are cleared.</p><h3 id="forcing-a-state-change" tabindex="-1">Forcing a state change <a class="header-anchor" href="#forcing-a-state-change" aria-label="Permalink to &quot;Forcing a state change&quot;">​</a></h3><p>These <code>EntityAspect</code> methods change the state directly:</p><table tabindex="0"><thead><tr><th>Method</th><th>Effect</th></tr></thead><tbody><tr><td><code>setDeleted()</code></td><td>Marks the entity for deletion. See <a href="#deleting">below</a>.</td></tr><tr><td><code>rejectChanges()</code></td><td>Reverts pending changes. See <a href="#rejectchanges">below</a>.</td></tr><tr><td><code>setModified()</code></td><td>Marks the entity <code>Modified</code>.</td></tr><tr><td><code>setUnchanged()</code></td><td>Marks the entity <code>Unchanged</code>, and clears its original values.</td></tr><tr><td><code>acceptChanges()</code></td><td>Like <code>setUnchanged()</code>, except that a <code>Deleted</code> entity is detached.</td></tr><tr><td><code>setAdded()</code></td><td>Marks the entity <code>Added</code>. Unlike <code>em.addEntity</code>, it doesn&#39;t generate a key.</td></tr><tr><td><code>setDetached()</code></td><td>Removes the entity from its manager.</td></tr><tr><td><code>setEntityState(state)</code></td><td>Any of the above, by value.</td></tr></tbody></table><p>Production code rarely calls <code>setModified</code>, <code>setUnchanged</code>, <code>setAdded</code> or <code>acceptChanges</code>, because entities reach those states as a side effect of normal work. They are most useful for putting test fixtures into a known state. Remember that <code>setUnchanged</code> and <code>acceptChanges</code> discard the original values, so the entity can no longer be reverted.</p><p><code>em.acceptChanges()</code> and <code>em.rejectChanges()</code> apply to every changed entity in the cache.</p><h3 id="deleting" tabindex="-1">Deleting <a class="header-anchor" href="#deleting" aria-label="Permalink to &quot;Deleting&quot;">​</a></h3><p>Deleting starts with a state change:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setDeleted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p><code>setDeleted</code> doesn&#39;t destroy the object, and doesn&#39;t touch the database. The entity stays in the cache as <code>Deleted</code> until you save. It is also removed from the collections of its related entities, and its scalar navigation properties are set to <code>null</code>, but its foreign key values are kept. A successful save deletes the entity from the database and removes it from the cache.</p><h3 id="rejectchanges" tabindex="-1">rejectChanges <a class="header-anchor" href="#rejectchanges" aria-label="Permalink to &quot;rejectChanges&quot;">​</a></h3><p>Once you change an entity, it stays changed, even if you set the old value back by hand:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> customer.companyName;              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Unchanged</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Something new&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Modified</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> name;                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// still Modified</span></span></code></pre></div><p><code>rejectChanges</code> cancels pending changes. It restores the original values, and sets the entity&#39;s state back to <code>Unchanged</code> (an <code>Added</code> entity becomes <code>Detached</code>):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Something new&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Modified</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rejectChanges</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Unchanged</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> name;                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true</span></span></code></pre></div><p>A deleted entity whose changes are rejected goes back into the collections it was removed from.</p><h3 id="original-values" tabindex="-1">Original values <a class="header-anchor" href="#original-values" aria-label="Permalink to &quot;Original values&quot;">​</a></h3><p>When you first change a property of an <code>Unchanged</code> or <code>Modified</code> entity, Breeze records the value it had before, in <code>entityAspect.originalValues</code>. That object is keyed by property name. It is empty while the entity is <code>Unchanged</code>, and it contains the properties edited since the last query or save - including one set back to where it started. Later changes to the same property don&#39;t overwrite the recorded value.</p><p>Two methods answer the usual questions without reading it directly:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.freight </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 99</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getOriginalValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;freight&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// what it was before the edit</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getOriginalValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;shipName&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// not edited: its current value</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getChangedProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [&#39;freight&#39;] - what differs now</span></span></code></pre></div><p><code>getChangedProperties()</code> compares values, so a property set back to its original value is not listed, and dates are compared by time. A complex property&#39;s changes are listed as paths, such as <code>&#39;location.city&#39;</code>, which <code>getOriginalValue</code> also takes.</p><p><strong>With a generated entity class they are typed.</strong> Its <code>entityAspect</code> is an <a href="/breeze-client-v3/api/interfaces/EntityAspectOf"><code>EntityAspectOf</code></a> the class, so property names are checked and values have their properties&#39; types:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> before</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getOriginalValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;freight&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.originalValues.freight;           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// number | undefined</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getOriginalValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;frieght&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// error: not a property of Order</span></span></code></pre></div><p><code>Added</code> entities don&#39;t record original values, since they have nothing to go back to. Original values for properties of a complex object are kept on the complex object&#39;s <code>complexAspect.originalValues</code> (see <a href="/breeze-client-v3/guide/complex-properties">Complex properties</a>).</p><p>These all replace <code>originalValues</code> with an empty object: a successful save, <code>rejectChanges</code>, <code>setUnchanged</code> and <code>acceptChanges</code>.</p><h2 id="propertychanged" tabindex="-1">propertyChanged <a class="header-anchor" href="#propertychanged" aria-label="Permalink to &quot;propertyChanged&quot;">​</a></h2><p>The <code>entityAspect.propertyChanged</code> event lets you listen for a change to any tracked property of an entity, with a single subscription:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> token</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.entityAspect.propertyChanged.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // args.entity, args.propertyName, args.oldValue, args.newValue</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// later</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.propertyChanged.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(token);</span></span></code></pre></div><p>A few things to know:</p><ul><li>Breeze monitors only the properties defined in metadata, both mapped and unmapped. It doesn&#39;t track properties you add to an entity outside the metadata.</li><li>Changes to <code>EntityAspect</code> properties don&#39;t raise the event. In particular, a change of <code>entityState</code> doesn&#39;t raise it. Listen to <code>em.entityChanged</code> for that.</li><li>Some operations update many properties at once: a query or save that merges new values into a cached entity, or <code>rejectChanges</code>. They raise a single event, with <code>propertyName</code> set to <code>null</code>.</li></ul><p><a href="/breeze-client-v3/guide/change-tracking">Change tracking</a> covers this event and the manager&#39;s <code>entityChanged</code> event in more detail.</p><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>Properties can be validated as they change, against the rules registered in metadata. Metadata from a Breeze .NET server already includes some of these rules: <code>required</code> for non-nullable properties, and <code>maxLength</code> for strings with a length limit. You can add your own as well. See <a href="/breeze-client-v3/guide/validation">Validation</a>.</p><p>You can also validate on demand:</p><table tabindex="0"><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>validateEntity()</code></td><td>Runs every property rule and every entity-level rule. Returns <code>true</code> if all pass.</td></tr><tr><td><code>validateProperty(property, context?)</code></td><td>Runs every rule for one property.</td></tr><tr><td><code>getValidationErrors(property?)</code></td><td>A copy of the current errors, for the entity or one property.</td></tr><tr><td><code>hasValidationErrors</code></td><td><code>true</code> while there are any errors.</td></tr><tr><td><code>addValidationError(error)</code></td><td>Adds a <a href="/breeze-client-v3/api/classes/ValidationError"><code>ValidationError</code></a> yourself.</td></tr><tr><td><code>removeValidationError(errorKeyOrValidator)</code></td><td>Removes one error, or every error a given validator produced.</td></tr><tr><td><code>clearValidationErrors()</code></td><td>Removes all of them.</td></tr><tr><td><code>validationErrorsChanged</code></td><td>An event raised when errors are added or removed.</td></tr></tbody></table><p>A rule either passes, or fails and produces a <code>ValidationError</code>. Breeze keeps the entity&#39;s current errors on its aspect. It adds an error when a rule fails, and removes it when the same rule later passes.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.entityAspect.validationErrorsChanged.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">entity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">added</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">removed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // added and removed are arrays of ValidationError</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="other-entityaspect-members" tabindex="-1">Other EntityAspect members <a class="header-anchor" href="#other-entityaspect-members" aria-label="Permalink to &quot;Other EntityAspect members&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>entity</code></td><td>The entity this aspect belongs to.</td></tr><tr><td><code>entityManager</code></td><td>The manager the entity is attached to, or was last attached to. Undefined for an entity that has never been attached.</td></tr><tr><td><code>getKey()</code></td><td>The entity&#39;s <a href="/breeze-client-v3/api/classes/EntityKey"><code>EntityKey</code></a>: an object holding the entity type and the key value, or values (Breeze supports composite keys).</td></tr><tr><td><code>hasTempKey</code></td><td><code>true</code> while the entity has a temporary key that the server will replace.</td></tr><tr><td><code>isBeingSaved</code></td><td><code>true</code> while a save that includes this entity is in progress. Calling <code>acceptChanges</code>, <code>rejectChanges</code> or <code>setEntityState</code> meanwhile throws.</td></tr><tr><td><code>loadNavigationProperty(name)</code></td><td>Queries the server for a navigation property&#39;s entities. See <a href="/breeze-client-v3/guide/navigation-properties">Navigation properties</a>.</td></tr><tr><td><code>isNavigationPropertyLoaded(name)</code></td><td>Whether that navigation property has been loaded.</td></tr><tr><td><code>getPropertyValue(path)</code></td><td>The value at a property path such as <code>&#39;customer.companyName&#39;</code> or <code>&#39;location.city&#39;</code>.</td></tr></tbody></table><h2 id="members-on-the-entity-itself" tabindex="-1">Members on the entity itself <a class="header-anchor" href="#members-on-the-entity-itself" aria-label="Permalink to &quot;Members on the entity itself&quot;">​</a></h2><p>Most of the Breeze infrastructure is reached through <code>entityAspect</code>, but Breeze also puts a few members on the entity&#39;s prototype:</p><ul><li><strong><code>entityType</code></strong>, the type&#39;s metadata, described <a href="#entitytype">above</a>.</li><li><strong><code>getProperty(name)</code></strong>, which returns a property&#39;s value.</li><li><strong><code>setProperty(name, value)</code></strong>, which sets it.</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;companyName&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Ima Something Corp&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;companyName&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;Ima Something Corp&#39;</span></span></code></pre></div><p>With the backing-store model library, <code>getProperty</code> and <code>setProperty</code> behave the same as reading and assigning the property directly. <code>setProperty</code> goes through the same path as an assignment, so it records original values, changes the entity state, validates and raises events in the same way. Code written with them doesn&#39;t need a TypeScript interface for the entity.</p><p><code>getProperty</code> takes a single property name, not a path. If <code>setProperty</code> gets a name that isn&#39;t in the metadata, it creates an ordinary untracked property instead of throwing, so check your spelling.</p><p>To use property syntax with type checking, describe your entities with interfaces that extend <a href="/breeze-client-v3/api/interfaces/Entity"><code>Entity</code></a>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Entity } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Order</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Entity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  orderID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  freight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  customer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Customer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  orderDetails</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderDetail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> order</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createEntity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Order&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">order.freight </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 12.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// tracked, just like setProperty</span></span></code></pre></div><p>The entity also carries internals such as <code>_backingStore</code> and <code>_$interceptor</code>. Leave them alone.</p>`,77)])])}const y=t(n,[["render",d]]);export{k as __pageData,y as default};
