import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.DYAmjhAm.js";const k=JSON.parse('{"title":"Optional extensions","description":"","frontmatter":{},"headers":[],"relativePath":"guide/extensions.md","filePath":"guide/extensions.md","lastUpdated":1789761571000}'),n={name:"guide/extensions.md"};function h(r,e,o,l,p,d){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="optional-extensions" tabindex="-1">Optional extensions <a class="header-anchor" href="#optional-extensions" aria-label="Permalink to &quot;Optional extensions&quot;">​</a></h1><p><code>breeze-client</code> is the whole library an application normally needs. The extensions on this page are extra, each in a subpath of its own, and none of them does anything until you import it. A bundler leaves out every one you do not import.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Extension</th><th>Import from</th><th>What it gives you</th><th>Needs</th><th>Acts on import</th></tr></thead><tbody><tr><td><a href="#save-queuing">Save queuing</a></td><td><code>breeze-client/mixin-save-queuing</code></td><td>a save made while another is in flight waits and follows it, and edits made during a save are kept</td><td>—</td><td>no: call <code>enableSaveQueuing(em, true)</code> for each manager</td></tr><tr><td><a href="#entity-graphs">Entity graphs</a></td><td><code>breeze-client/mixin-get-entity-graph</code></td><td>an entity and everything an expand path reaches from it, <strong>including deleted entities</strong>, from the cache</td><td>—</td><td><strong>yes</strong>: adds <code>getEntityGraph</code> to every <code>EntityManager</code></td></tr><tr><td><a href="#rxjs">RxJS</a></td><td><code>breeze-client/rxjs</code></td><td>Breeze events as RxJS observables</td><td><code>rxjs</code>, which you install</td><td>no</td></tr><tr><td><a href="#angular-httpclient">Angular HttpClient</a></td><td><code>breeze-client/adapter-angular-httpclient</code></td><td>Breeze&#39;s requests sent through Angular&#39;s <code>HttpClient</code>, so its interceptors see them</td><td><code>@angular/common</code> and <code>rxjs</code>, which an Angular application already has</td><td>no</td></tr></tbody></table><p><strong>Needs</strong> is what the extension asks you to install. <code>breeze-client</code> itself installs nothing else, and none of these change that: RxJS and <code>@angular/common</code> are <em>optional</em> peer dependencies, so npm does not install them for you, and an application that does not import the extension that uses them never gets them. The test suite checks that nothing reachable from <code>breeze-client</code> imports either one, not even for a type.</p><p><strong>Acts on import</strong> matters for tree-shaking. Entity graphs is the one extension that changes something just by being imported, which is why it is the one module named in the package&#39;s <code>sideEffects</code>. The others are functions: importing them does nothing until you call one.</p><h2 id="save-queuing" tabindex="-1">Save queuing <a class="header-anchor" href="#save-queuing" aria-label="Permalink to &quot;Save queuing&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { enableSaveQueuing } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/mixin-save-queuing&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Some applications save automatically after every edit, and a user can easily make a second change before the first save returns. Save queuing handles that: while a save is in flight, further <code>saveChanges</code> calls are held back and sent as one follow-up save when the first returns.</p><p>It also does the thing you would otherwise have to do yourself — <strong>it keeps edits made while the save was out</strong>. It records what changed on each entity being saved, re-applies those values over the server&#39;s response, and sends them in the follow-up save. Without it they are <a href="/breeze-client-v3/guide/saving-changes#changes-during-a-save">quietly overwritten</a>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cust.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Second&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">saveChanges</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// in flight</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cust.companyName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Third&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the user keeps typing</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// without queuing: companyName is back to &#39;Second&#39; when the save returns</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// with queuing:    &#39;Third&#39;, and &#39;Third&#39; is what the database ends up holding</span></span></code></pre></div><p>A foreign key set during a save is handled too: if you point a child at a parent whose row is still being inserted, the only key you have is the temporary one, and the queued save substitutes the key the server assigned.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">enableSaveQueuing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(em, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> p1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">saveChanges</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sent now</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">editSomething</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> p2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">saveChanges</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// queued; sent when p1&#39;s save returns</span></span></code></pre></div><p>Each promise resolves with the result of the save that included its changes. If a queued save fails, every pending promise rejects with a <a href="/breeze-client-v3/api/classes/QueuedSaveFailedError"><code>QueuedSaveFailedError</code></a>. Its <code>innerError</code> is the underlying error.</p><p>Limitations:</p><ul><li>The <code>SaveOptions</code> of the first save are reused for the queued saves.</li><li>Only the promise form works. The deprecated callback arguments are ignored.</li><li>It does not queue parallel saves, even of independent change-sets.</li><li>It is meant for short-latency auto-save. It does not help with offline work, and it does not cope with <code>rejectChanges</code>, export/import or primary-key changes while a save is in flight.</li></ul><p>Turn it off again with <code>enableSaveQueuing(em, false)</code>. Calling it more than once on the same manager is harmless. Reference: <a href="/breeze-client-v3/api/functions/enableSaveQueuing"><code>enableSaveQueuing</code></a>.</p><h2 id="entity-graphs" tabindex="-1">Entity graphs <a class="header-anchor" href="#entity-graphs" aria-label="Permalink to &quot;Entity graphs&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/mixin-get-entity-graph&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { HasEntityGraph } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/mixin-get-entity-graph&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>getEntityGraph</code> returns an entity together with everything an expand path reaches from it, from the cache, without going to the server:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> graph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (em </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HasEntityGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getEntityGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(customer, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orders.orderDetails&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// customer, its orders, and their details</span></span></code></pre></div><p>What it does that walking the navigation properties yourself does not:</p><ul><li><strong>It includes deleted entities.</strong> Deleting an order takes it out of <code>customer.orders</code>, but not out of the graph — and not its details either. That is the list to hand to <code>em.saveChanges(graph)</code> when you want to save a customer&#39;s changes <em>including</em> what was deleted.</li><li><strong>It is linear in what it returns.</strong> Children are found by indexing them on their foreign key once per expand segment, so a customer with 8,000 orders and 24,000 details takes about 10 ms.</li></ul><p>It takes one root or an array of them, and an expand path in any form a query accepts — a string, an array of strings, or an <code>ExpandClause</code>. It also takes a query instead of roots, which it runs against the cache, using the query&#39;s own <code>expand</code> unless you pass one:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> q</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EntityQuery.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Customers&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;companyName&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;startsWith&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Alf&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orders&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> graph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (em </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HasEntityGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getEntityGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(q);</span></span></code></pre></div><p>The result has no duplicates and no particular order. An expand segment that names something that is not a navigation property throws, naming the segment and the type: <code>&#39;getEntityGraph&#39; can&#39;t expand &#39;noSuchProperty&#39; for Customer:#Foo</code>.</p><h3 id="importing-it" tabindex="-1">Importing it <a class="header-anchor" href="#importing-it" aria-label="Permalink to &quot;Importing it&quot;">​</a></h3><p>The import on its own is enough: it adds <code>getEntityGraph</code> to <code>EntityManager.prototype</code>, for every manager. <code>HasEntityGraph</code> is the type of a manager that has it. TypeScript cannot see what an import added to a class, so cast, or declare your manager as <code>HasEntityGraph</code> where you create it.</p><p>Because an import that only has an effect is exactly what an aggressive bundler may drop, <code>breeze-client</code> lists this module in <code>sideEffects</code> so that bundlers keep it. If yours drops it anyway, call the function yourself — adding it twice is harmless:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mixinEntityGraph } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/mixin-get-entity-graph&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mixinEntityGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(EntityManager);</span></span></code></pre></div><p>Reference: <a href="/breeze-client-v3/api/interfaces/HasEntityGraph"><code>HasEntityGraph</code></a>, <a href="/breeze-client-v3/api/functions/mixinEntityGraph"><code>mixinEntityGraph</code></a>.</p><h2 id="rxjs" tabindex="-1">RxJS <a class="header-anchor" href="#rxjs" aria-label="Permalink to &quot;RxJS&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { entityChanged$, hasChanges$ } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/rxjs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Breeze&#39;s events — <code>entityChanged</code>, <code>hasChangesChanged</code>, <code>propertyChanged</code> and the rest — as RxJS observables, so they compose with the rest of an application&#39;s streams and tear down with its other subscriptions. <code>hasChanges$</code> starts with the manager&#39;s current value, which is what a Save button needs and what a hand-written <code>BehaviorSubject(false)</code> gets wrong.</p><p>Install <code>rxjs</code> yourself; any version from 7.0 on works.</p><p>It has a page of its own, because the part that needs care — unsubscribing, and sharing one subscription without leaking it — takes more than a paragraph: <strong><a href="/breeze-client-v3/guide/rxjs">RxJS</a></strong>.</p><h2 id="angular-httpclient" tabindex="-1">Angular HttpClient <a class="header-anchor" href="#angular-httpclient" aria-label="Permalink to &quot;Angular HttpClient&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { httpClientFetch } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/adapter-angular-httpclient&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Breeze makes its requests with <code>fetch</code>, so Angular&#39;s <code>HttpClient</code> interceptors — the auth header, logging, retry — never see them, and neither does <code>HttpTestingController</code>. <code>httpClientFetch</code> turns an <code>HttpClient</code> into the <code>fetch</code> Breeze calls:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideAppInitializer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  configureBreeze</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ fetch: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">httpClientFetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(HttpClient)) });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}),</span></span></code></pre></div><p>The two report a server error differently. When the server answers with a 4xx or 5xx status, <code>fetch</code> still returns the response, and Breeze reads its status and body. <code>HttpClient</code> throws an <code>HttpErrorResponse</code> instead. Breeze treats a thrown error as a request that never reached the server, so without the extension a save the server rejected would lose its <code>entityErrors</code>, and a 409 would not be recognised as a concurrency conflict. <code>httpClientFetch</code> catches the <code>HttpErrorResponse</code> and turns it back into the response <code>fetch</code> would have returned. When <code>HttpClient</code> reports status 0, meaning there was no response at all, it rethrows the error, and Breeze reports a transport failure just as it would with <code>fetch</code>.</p><p>It is the replacement for 2.x&#39;s <code>AjaxHttpClientAdapter</code>. Any Angular version from 13 on works; it uses only the types of <code>@angular/common/http</code>, so it adds no second copy of Angular to your bundle. The <a href="/breeze-client-v3/guide/angular#requests-and-the-auth-header">Angular guide</a> covers when to use it rather than a plain <code>fetch</code> that adds the header itself. Reference: <a href="/breeze-client-v3/api/functions/httpClientFetch"><code>httpClientFetch</code></a>.</p>`,42)])])}const g=s(n,[["render",h]]);export{k as __pageData,g as default};
