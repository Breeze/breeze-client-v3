import{_ as t,o as a,c as i,a2 as s}from"./chunks/framework.DYAmjhAm.js";const g=JSON.parse('{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1789761211000}'),d={name:"guide/configuration.md"};function n(r,e,o,c,h,l){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h1><p>Breeze needs no configuration for a Breeze .NET server:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { EntityManager } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> em</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EntityManager</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/breeze/NorthwindIBModel&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Every setting has a default, listed below. Change the ones you need at startup, before you create any <code>MetadataStore</code> or <code>EntityManager</code>: stores and managers take the defaults in force when they are created.</p><h2 id="what-you-can-import" tabindex="-1">What you can import <a class="header-anchor" href="#what-you-can-import" aria-label="Permalink to &quot;What you can import&quot;">​</a></h2><p><code>breeze-client</code> holds everything a normal application needs. The rest are separate entry points so that a bundler leaves out what you do not import. The optional extensions among them — save queuing, entity graphs, RxJS and the Angular <code>HttpClient</code> bridge — are described together in <a href="/breeze-client-v3/guide/extensions">Optional extensions</a>.</p><table tabindex="0"><thead><tr><th>Import from</th><th>What it is</th></tr></thead><tbody><tr><td><code>breeze-client</code></td><td><code>EntityManager</code>, <code>EntityQuery</code>, <code>MetadataStore</code>, <code>Predicate</code>, <code>Validator</code>, <code>configureBreeze</code> — the library</td></tr><tr><td><code>breeze-client/mixin-save-queuing</code></td><td><a href="/breeze-client-v3/api/functions/enableSaveQueuing"><code>enableSaveQueuing</code></a> — queue a save made while another is in flight, and keep edits made during it. See <a href="/breeze-client-v3/guide/extensions#save-queuing">Save queuing</a></td></tr><tr><td><code>breeze-client/mixin-get-entity-graph</code></td><td><a href="/breeze-client-v3/api/functions/mixinEntityGraph"><code>mixinEntityGraph</code></a>, <a href="/breeze-client-v3/api/interfaces/HasEntityGraph"><code>HasEntityGraph</code></a> — adds <code>em.getEntityGraph(roots, expand)</code>. See <a href="/breeze-client-v3/guide/extensions#entity-graphs">Entity graphs</a></td></tr><tr><td><code>breeze-client/rxjs</code></td><td><a href="/breeze-client-v3/api/functions/hasChanges$"><code>hasChanges$</code></a>, <a href="/breeze-client-v3/api/functions/entityChanged$"><code>entityChanged$</code></a> and the rest — Breeze events as RxJS observables. Needs <code>rxjs</code>, which you install yourself. See <a href="/breeze-client-v3/guide/rxjs">RxJS</a></td></tr><tr><td><code>breeze-client/adapter-angular-httpclient</code></td><td><a href="/breeze-client-v3/api/functions/httpClientFetch"><code>httpClientFetch</code></a> — sends Breeze&#39;s requests through Angular&#39;s <code>HttpClient</code>, so its interceptors see them. Needs <code>@angular/common</code>. See <a href="/breeze-client-v3/guide/extensions#angular-httpclient">Angular HttpClient</a></td></tr><tr><td><code>breeze-client/adapter-model-library-backing-store</code></td><td>The default model library adapter</td></tr><tr><td><code>breeze-client/adapter-data-service-webapi</code></td><td>The default data service adapter</td></tr><tr><td><code>breeze-client/adapter-uri-builder-json</code></td><td>The default URI builder</td></tr><tr><td><code>breeze-client/adapter-ajax-fetch</code></td><td>The deprecated ajax adapter — see <a href="/breeze-client-v3/server/transport">Supplying your own transport</a></td></tr><tr><td><code>breeze-client/adapter-ajax-post</code></td><td>Sends queries as <code>POST</code>, for queries too long for a URL</td></tr></tbody></table><p>The four adapter subpaths are the defaults, so you only import one to subclass it or to <a href="#the-older-api-still-works">register it explicitly</a>. The two mixins are opt-in: nothing pulls them in unless you import them.</p><h2 id="defaults" tabindex="-1">Defaults <a class="header-anchor" href="#defaults" aria-label="Permalink to &quot;Defaults&quot;">​</a></h2><h3 id="adapters-and-transport" tabindex="-1">Adapters and transport <a class="header-anchor" href="#adapters-and-transport" aria-label="Permalink to &quot;Adapters and transport&quot;">​</a></h3><p>Breeze delegates three jobs to adapters, and makes its HTTP requests through a <code>fetch</code> function.</p><table tabindex="0"><thead><tr><th>Interface</th><th>Job</th><th>Default</th><th>To replace it</th></tr></thead><tbody><tr><td><code>modelLibrary</code></td><td>how entities track changes</td><td><code>ModelLibraryBackingStoreAdapter</code> (<code>&#39;backingStore&#39;</code>)</td><td><code>configureBreeze({ modelLibrary })</code></td></tr><tr><td><code>dataService</code></td><td>how to talk to the server</td><td><code>DataServiceWebApiAdapter</code> (<code>&#39;webApi&#39;</code>)</td><td><code>configureBreeze({ dataService })</code> — see <a href="/breeze-client-v3/server/dataserviceadapter">DataServiceAdapter</a></td></tr><tr><td><code>uriBuilder</code></td><td>how a query becomes a URL</td><td><code>UriBuilderJsonAdapter</code> (<code>&#39;json&#39;</code>)</td><td><code>configureBreeze({ uriBuilder })</code></td></tr><tr><td>HTTP</td><td>makes the request</td><td>no ajax adapter: <code>config.fetch</code>, which defaults to <code>globalThis.fetch</code></td><td><code>configureBreeze({ fetch })</code> — see <a href="/breeze-client-v3/server/transport">Supplying your own transport</a></td></tr></tbody></table><p>Breeze registers a default adapter the first time it needs it, and only for an interface that nothing has been registered for. Nothing is registered when you import Breeze, and anything you register takes precedence. A <code>DataService</code> resolves its adapters once, the first time it is used, and keeps them.</p><h3 id="global-settings" tabindex="-1">Global settings <a class="header-anchor" href="#global-settings" aria-label="Permalink to &quot;Global settings&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Setting</th><th>Default</th><th>To change it</th></tr></thead><tbody><tr><td>Naming convention</td><td><code>NamingConvention.camelCase</code>: <code>CompanyName</code> on the server is <code>companyName</code> on the client</td><td><code>configureBreeze({ namingConvention })</code>, or <code>NamingConvention.none.setAsDefault()</code>. See <a href="#naming-conventions">Naming conventions</a></td></tr><tr><td>Local string comparison</td><td><code>LocalQueryComparisonOptions.caseInsensitiveSQL</code>: case-insensitive, and <code>==</code> / <code>!=</code> ignore leading and trailing spaces</td><td><code>new LocalQueryComparisonOptions({ ... }).setAsDefault()</code>. See <a href="/breeze-client-v3/query/locally">Querying the cache</a></td></tr><tr><td>Query options</td><td><code>fetchStrategy: FetchStrategy.FromServer</code>, <code>mergeStrategy: MergeStrategy.PreserveChanges</code>, <code>includeDeleted: false</code></td><td><code>new QueryOptions({ ... }).setAsDefault()</code>; per manager with <code>queryOptions</code>; per query with <code>EntityQuery.using(...)</code></td></tr><tr><td>Save options</td><td><code>allowConcurrentSaves: false</code>; saves go to <code>SaveChanges</code> on the manager&#39;s service</td><td><code>new SaveOptions({ ... }).setAsDefault()</code>; per manager with <code>saveOptions</code>; per save as the second argument to <code>saveChanges</code></td></tr><tr><td>Validation options</td><td><code>validateOnAttach: true</code>, <code>validateOnSave: true</code>, <code>validateOnQuery: false</code>, <code>validateOnPropertyChange: true</code></td><td><code>new ValidationOptions({ ... }).setAsDefault()</code>; per manager with <code>validationOptions</code></td></tr><tr><td>Transport</td><td><code>config.fetch</code> is unset, so requests use <code>globalThis.fetch</code></td><td><code>configureBreeze({ fetch })</code>, or assign <code>config.fetch</code></td></tr></tbody></table><p><code>setAsDefault()</code> changes the default for everything created afterwards. It does not change stores and managers that already exist.</p><h3 id="entitymanager" tabindex="-1">EntityManager <a class="header-anchor" href="#entitymanager" aria-label="Permalink to &quot;EntityManager&quot;">​</a></h3><p><code>new EntityManager(serviceName)</code>, or <code>new EntityManager({ ... })</code> with any of:</p><table tabindex="0"><thead><tr><th>Option</th><th>Default</th></tr></thead><tbody><tr><td><code>serviceName</code></td><td>none</td></tr><tr><td><code>dataService</code></td><td>the <code>MetadataStore</code>&#39;s <code>DataService</code> for <code>serviceName</code>, if it has one; otherwise a new <code>DataService</code> for that name</td></tr><tr><td><code>metadataStore</code></td><td>a new <code>MetadataStore</code>, with the default naming convention and comparison options</td></tr><tr><td><code>queryOptions</code>, <code>saveOptions</code>, <code>validationOptions</code></td><td>the defaults in force when the manager is created</td></tr><tr><td><code>keyGeneratorCtor</code></td><td><code>KeyGenerator</code></td></tr></tbody></table><h3 id="dataservice" tabindex="-1">DataService <a class="header-anchor" href="#dataservice" aria-label="Permalink to &quot;DataService&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Option</th><th>Default</th></tr></thead><tbody><tr><td><code>serviceName</code></td><td>required</td></tr><tr><td><code>hasServerMetadata</code></td><td><code>true</code>: metadata is fetched from <code>{serviceName}/Metadata</code> before the first query</td></tr><tr><td><code>adapterName</code></td><td>the default data service adapter (<code>&#39;webApi&#39;</code>)</td></tr><tr><td><code>uriBuilderName</code></td><td>the default URI builder (<code>&#39;json&#39;</code>)</td></tr><tr><td><code>jsonResultsAdapter</code></td><td>the data service adapter&#39;s own</td></tr><tr><td><code>useJsonp</code></td><td><code>false</code>. Deprecated: no effect on Breeze’s own transport</td></tr></tbody></table><h3 id="metadatastore" tabindex="-1">MetadataStore <a class="header-anchor" href="#metadatastore" aria-label="Permalink to &quot;MetadataStore&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Option</th><th>Default</th></tr></thead><tbody><tr><td><code>namingConvention</code></td><td>the default naming convention when the store is created. An empty store that imports metadata naming a convention adopts that one</td></tr><tr><td><code>localQueryComparisonOptions</code></td><td>the default comparison options when the store is created. An empty store adopts the ones named in imported metadata, unless the store was given options of its own</td></tr></tbody></table><h2 id="configurebreeze" tabindex="-1">configureBreeze <a class="header-anchor" href="#configurebreeze" aria-label="Permalink to &quot;configureBreeze&quot;">​</a></h2><p><code>configureBreeze</code> is optional. Use it to replace a default adapter, supply your own <code>fetch</code>, or set the naming convention:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { configureBreeze } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MyWebApiAdapter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./my-web-api-adapter&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// a subclass of DataServiceWebApiAdapter</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">configureBreeze</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  dataService: MyWebApiAdapter,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  fetch: authFetch,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The standard adapters are still exported, from <code>breeze-client/adapter-model-library-backing-store</code>, <code>breeze-client/adapter-uri-builder-json</code> and <code>breeze-client/adapter-data-service-webapi</code>, so that you can subclass them or register them explicitly. Passing one to <code>configureBreeze</code> does no harm, but it is not needed.</p><h3 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>modelLibrary</code></td><td>adapter class</td><td>replaces the default, <code>ModelLibraryBackingStoreAdapter</code></td></tr><tr><td><code>uriBuilder</code></td><td>adapter class</td><td>replaces the default, <code>UriBuilderJsonAdapter</code></td></tr><tr><td><code>ajax</code></td><td>adapter class</td><td>deprecated — see <a href="#ajax-deprecated">below</a></td></tr><tr><td><code>dataService</code></td><td>adapter class</td><td>replaces the default, <code>DataServiceWebApiAdapter</code></td></tr><tr><td><code>fetch</code></td><td><code>BreezeFetch</code></td><td>the function every HTTP request goes through; defaults to <code>globalThis.fetch</code>. See <a href="/breeze-client-v3/server/transport">Supplying your own transport</a></td></tr><tr><td><code>namingConvention</code></td><td><code>NamingConvention</code></td><td>sets the default, which is initially <code>NamingConvention.camelCase</code></td></tr><tr><td><code>config</code></td><td><code>BreezeConfig</code></td><td>target a non-global config; rarely needed</td></tr></tbody></table><p>You can call it more than once and pass only what you want to change:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">configureBreeze</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ namingConvention: NamingConvention.none });</span></span></code></pre></div><h3 id="registration-order-does-not-matter" tabindex="-1">Registration order does not matter <a class="header-anchor" href="#registration-order-does-not-matter" aria-label="Permalink to &quot;Registration order does not matter&quot;">​</a></h3><p>Instead of passing an adapter to <code>configureBreeze</code>, you can call its static <code>register()</code>. Adapters can be registered in any order:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { DataServiceWebApiAdapter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/adapter-data-service-webapi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { UriBuilderJsonAdapter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/adapter-uri-builder-json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Either order works. So does leaving both out: these are the defaults.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">UriBuilderJsonAdapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DataServiceWebApiAdapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>What does matter is registering before you create an <code>EntityManager</code> — see <a href="#adapters-and-transport">Default adapters</a>.</p><h2 id="importing-does-not-register" tabindex="-1">Importing does not register <a class="header-anchor" href="#importing-does-not-register" aria-label="Permalink to &quot;Importing does not register&quot;">​</a></h2><p>In 2.x, importing an adapter module registered it as a side effect:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2.x — the import alone was enough</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;breeze-client/adapter-ajax-fetch&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Breeze 3 modules do not touch global state when imported. An adapter is registered only when you pass it to <code>configureBreeze</code> or call its <code>register()</code>, or when Breeze falls back to a default.</p><p>This makes import order irrelevant and lets bundlers reason about the package properly. It rarely matters otherwise: the adapters 2.x registered on import are the defaults, and need no registration at all. See <a href="/breeze-client-v3/guide/migrating-from-2x">Migrating from 2.x</a>.</p><h2 id="the-older-api-still-works" tabindex="-1">The older API still works <a class="header-anchor" href="#the-older-api-still-works" aria-label="Permalink to &quot;The older API still works&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dataService&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, DataServiceWebApiAdapter);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">initializeAdapterInstance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dataService&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webApi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>registerAdapter</code>, <code>initializeAdapterInstance</code> and <code>initializeAdapterInstances</code> are all still there and still work. They are marked <code>@deprecated</code> because <code>configureBreeze</code> is better — a misspelled adapter name is a compile error rather than a runtime one — but they are not scheduled for removal.</p><p>You don&#39;t need <code>registerAdapter</code> before initializing a default adapter by name. The names <code>&#39;backingStore&#39;</code>, <code>&#39;json&#39;</code> and <code>&#39;webApi&#39;</code>, and the ajax adapter&#39;s <code>&#39;fetch&#39;</code>, resolve to the defaults, so 2.x startup code that relied on the import side effect runs as-is:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">initializeAdapterInstance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dataService&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webApi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">initializeAdapterInstance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ajax&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fetch&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">initializeAdapterInstances</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ dataService: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webApi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ajax: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fetch&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><p><code>&#39;fetch&#39;</code> gives an <code>AjaxFetchAdapter</code> that sends its requests through <code>config.fetch</code>. A name that is neither registered nor a default still throws <code>Unregistered adapter</code>.</p><h2 id="choosing-adapters" tabindex="-1">Choosing adapters <a class="header-anchor" href="#choosing-adapters" aria-label="Permalink to &quot;Choosing adapters&quot;">​</a></h2><h3 id="modellibrary" tabindex="-1">modelLibrary <a class="header-anchor" href="#modellibrary" aria-label="Permalink to &quot;modelLibrary&quot;">​</a></h3><p><code>ModelLibraryBackingStoreAdapter</code> is the default and the only one shipped, and the right choice for plain JavaScript and TypeScript models. The Knockout adapter was removed in 3.0.</p><h3 id="dataservice-1" tabindex="-1">dataService <a class="header-anchor" href="#dataservice-1" aria-label="Permalink to &quot;dataService&quot;">​</a></h3><p><code>DataServiceWebApiAdapter</code>, the default, talks to a Breeze .NET server, or anything using the same JSON shape. To target a different backend, subclass <code>AbstractDataServiceAdapter</code> and register it — see <a href="/breeze-client-v3/server/dataserviceadapter">DataServiceAdapter</a>.</p><p>The OData data service adapter was removed in 3.0.</p><h3 id="uribuilder" tabindex="-1">uriBuilder <a class="header-anchor" href="#uribuilder" aria-label="Permalink to &quot;uriBuilder&quot;">​</a></h3><p><code>UriBuilderJsonAdapter</code>, the default, encodes the query as Breeze JSON in the query string. It is the only one shipped; the OData URI builder was removed in 3.0.</p><h3 id="ajax-deprecated" tabindex="-1">ajax (deprecated) <a class="header-anchor" href="#ajax-deprecated" aria-label="Permalink to &quot;ajax (deprecated)&quot;">​</a></h3><p>Breeze 3 does not need an ajax adapter. Every request goes through <code>config.fetch</code>, which defaults to <code>globalThis.fetch</code>. To add auth headers, retry or logging, supply your own <code>fetch</code> — see <a href="/breeze-client-v3/server/transport">Supplying your own transport</a>.</p><p><code>AjaxFetchAdapter</code> and the <code>ajax</code> option are kept so that 2.x startup code keeps working. If you register an ajax adapter, Breeze uses it instead of <code>config.fetch</code>. There is no reason to in new code.</p><h2 id="naming-conventions" tabindex="-1">Naming conventions <a class="header-anchor" href="#naming-conventions" aria-label="Permalink to &quot;Naming conventions&quot;">​</a></h2><p>A <code>NamingConvention</code> translates between server and client property names. The default is <code>NamingConvention.camelCase</code>.</p><ul><li><code>NamingConvention.camelCase</code> — <code>CompanyName</code> ⟷ <code>companyName</code>. The default, and what a .NET server needs.</li><li><code>NamingConvention.none</code> — names pass through unchanged. Use it when the server already sends the names the client should use:</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">configureBreeze</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ namingConvention: NamingConvention.none });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NamingConvention.none.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAsDefault</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>Metadata that names a naming convention sets it when imported into an empty <code>MetadataStore</code>. See <a href="/breeze-client-v3/server/namingconvention">Naming conventions</a> for custom conventions.</p><h2 id="content-security-policy" tabindex="-1">Content Security Policy <a class="header-anchor" href="#content-security-policy" aria-label="Permalink to &quot;Content Security Policy&quot;">​</a></h2><p>Breeze needs no Content Security Policy exception. It never evaluates a string - no <code>eval</code>, no <code>new Function</code> - so a policy without <code>&#39;unsafe-eval&#39;</code> runs it without complaint and without CSP violation reports.</p>`,64)])])}const k=t(d,[["render",n]]);export{g as __pageData,k as default};
